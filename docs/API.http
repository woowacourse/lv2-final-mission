### 방 생성
POST http://localhost:8080/room
Content-Type: application/json

{
  "startDate": "2025-05-20",
  "endDate": "2025-05-30",
  "startTime": "08:00",
  "endTime": "22:00",
  "isAnonymousRoom": true
}

### 방 정보 확인
GET http://localhost:8080/room/{{roomId}}

### 투표자 추가
POST http://localhost:8080/voter/{{roomId}}
Content-Type: application/json

{
  "name": "{{name}}",
  "password": "{{password}}"
}

### 익명 투표자 추가
POST http://localhost:8080/voter/{{roomId}}
Content-Type: application/json

{
  "password": "{{password}}"
}

### 투표
POST http://localhost:8080/vote/{{roomId}}?
    name={{name}}&
    password={{password}}
Content-Type: application/json

{
  "values": [
    "2025-05-21T10:00",
    "2025-05-21T11:00",
    "2025-05-21T12:00"
  ]
}

### 내 투표 확인
GET http://localhost:8080/vote/{{roomId}}/my?
    name={{name}}&
    password={{password}}

### 투표 통계 확인
GET http://localhost:8080/vote/{{roomId}}

### 내 투표 제거
DELETE http://localhost:8080/vote/{{roomId}}?
    name={{name}}&
    password={{password}}