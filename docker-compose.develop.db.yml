version: "3.8"

services:
  db:
    image: "mysql:8"
    container_name: mysql-db
    ports:
      - "3306:3306"
    env_file:
      - .env.develop
    volumes:
      - db-data:/var/lib/mysql
volumes:
  db-data:
