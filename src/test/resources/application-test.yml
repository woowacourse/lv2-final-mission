spring:
  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database: mysql
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create

  datasource:
    url: jdbc:mysql://localhost:3306/lv2_final?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 11111111

  sql:
    init:
      mode: never
