spring:
  config:
    activate:
      on-profile: test
  sql:
    init:
      mode: always
      data-locations:
  h2.console:
    enabled: true
    path: /h2-console
  datasource.url: jdbc:h2:mem:database

auth:
  jwt:
    secret-key: test-secret-key-for-unit-test-unit-test-unit-test
    valid-time: 2000000
  cookie:
    max-age: 1800
