spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST}:${DB_PORT:3306}/${DB_NAME}?useSSL=true&requireSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: false
  profiles:
    active: local
  h2:
    console:
      enabled: false
  jwt:
    secret: iwantcoffee-americano+ice-americano-is-best-i.love_it+yaho
    expirationMinute: 15
payment:
  secretKey: test_gsk_docs_OaPz8L5KdmQXkzRz3y47BMw6
